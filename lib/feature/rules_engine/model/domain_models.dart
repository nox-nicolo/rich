// lib/feature/rules_engine/model/domain_models.dart
//
// Convenience barrel — re-exports the two most commonly imported
// rules-engine types so callers only need one import.

export 'rich_feature.dart';
export 'user_mode.dart';
